server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # The HTML shell names the current hashed JS/CSS files. Never let an embedded
    # WebView reuse an old shell after deploy: it would keep loading the old
    # bundle even though the new asset files have different names.
    location = /index.html {
        add_header Cache-Control "no-cache, must-revalidate" always;
    }

    # SPA fallback. The exact /index.html location above also covers internal
    # fallbacks, while this header makes the root and client-side routes explicit.
    location / {
        try_files $uri /index.html;
        add_header Cache-Control "no-cache, must-revalidate" always;
    }

    # API проксировать, если нужно
    location /api/ {
        try_files $uri /index.html;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Optional: кэширование статических файлов
    location ~* \.(?:ico|css|js|gif|jpe?g|png|woff2?|eot|ttf|svg)$ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public";
    }
}
