export default function PageLoader() {
  return (
    <div className="min-h-viewport flex items-center justify-center">
      <div className="h-10 w-10 animate-spin rounded-full border-[3px] border-accent-500 border-t-transparent" />
    </div>
  );
}
